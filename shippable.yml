language: none
branches:
  only:
    - master
    - release/*
    - shippable/*
build:
  ci:
    - export IMAGE_TAG="${BRANCH#*/}"
    - export CARGO_FLAGS="--release"
    - docker build --build-arg CARGO_FLAGS=$CARGO_FLAGS -t hato-project/hato:$IMAGE_TAG .
  post_ci:
    # docker push hato-project/hato:$IMAGE_TAG
